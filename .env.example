# Sample environment variables configuration - rename to .env
# MongoDB Connection
# For MongoDB Atlas, use the connection string format:
MONGO_URI=mongo://<db_user>:<db_password>@<host>:<port>/<database>?retryWrites=true&w=majority
# For local MongoDB, use the connection string format:
# MONGO_URI=mongodb://localhost:27017/localbytes

# Server Configuration
PORT=5000
NODE_ENV=development

# Client URL (for CORS)
CLIENT_URL=http://localhost:4200

# Google Maps API Key
GMAPS_API_KEY=your_google_maps_api_key
